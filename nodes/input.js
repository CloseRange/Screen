class IO {
  constructor(nodeBase, dir, cellX, cellY, scaleX, scaleY, offsetX, offsetY) {
    this.nodeBase = nodeBase;
    this.dir = dir;
    this.cellX = cellX;
    this.cellY = cellY;
    this.scaleX = scaleX;
    this.scaleY = scaleY;
    this.offsetX = offsetX;
    this.offsetY = offsetY;
    drawable(THIS);
  }
  draw() {
    
  }
}
